import React from "react";

const Header: React.FC = () => {
	return (
		<div className="flex justify-center items-center w-full bg-blue-200 h-12">
			The Nash Calculator (In Progress)
		</div>
	);
};

export default Header;
